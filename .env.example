# Network Authentication
# Note: If the code starts with #, ensure it's quoted if your parser requires it,
# but most Next.js env parsers will handle it if it's the whole value.
# To be safe, we'll use a value without a leading # or quote it.
NEXT_PUBLIC_NETWORK_AUTH_CODE="ARCH_7701_VOID"
